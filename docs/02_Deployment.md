# Deploying on Chrome
1. Run `npm run build`
2. Navigate to the `./release` folder to locate the generated zip file

# Deploying on Firefox
1. Run `npm run build:firefox`
2. Zip all of the output that is generated in the the `./dist` folder
3. The zipped folder is the extension release